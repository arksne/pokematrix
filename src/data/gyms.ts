/**
 * ============================================================
 * gyms.ts — ЛИДЕРЫ ЗАЛОВ (gymLeaders)
 * ============================================================
 * 🔹 Структура: gymLeaders[locationId] = { name, types, team, badgeId, ... }
 * 🔹 Используется: battle/core.ts (залы), getters.ts (для UI)
 * 🔹 Зависит: ничего
 * ============================================================
 */
export const gymLeaders = {
  // ── Брок — Лидер Зала Пьютера (rock) ──
