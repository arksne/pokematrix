// ─────────────────────────────────────────────────────────────
// gyms.ts — ЛИДЕРЫ ЗАЛОВ
// ─────────────────────────────────────────────────────────────
// gymLeaders — словарь: ID локации → описание лидера зала.
// Каждый лидер:
//   name: str        — имя (Брок, Мисти, ...)
//   types: [str]     — типы покемонов лидера (rock, water, ...)
//   team: [PokeDef]  — команда покемонов лидера (вид, уровень, атаки)
//   badgeId: str     — ID значка, который даётся после победы
//   dialogue: {...}  — фразы до/после/во время боя
//   location: str    — город зала
//
// Используется в:
//   battle/core.ts   — загрузка команды лидера при GymBattle
//   getters.ts       — getGameState().gymLeaders (для UI)
//   init.ts          — проверка доступности залов
//   gym-reward.ts    — выбор награды после победы
//
// Зависит: ничего (чистые данные)
// ─────────────────────────────────────────────────────────────
export const gymLeaders = {
  // ── Брок — Лидер Зала Пьютера (rock) ──
