/**
 * ============================================================
 * config.ts — КОНФИГУРАЦИЯ ИГРЫ
 * ============================================================
 *
 * 🔹 ЧТО ДЕЛАЕТ:
 *   Определяет базовые константы: URL API, тайминги.
 *   Единственное место, где настраиваются endpoints и задержки.
 *
 * 🔹 ЗАВИСИМОСТИ (импорты):
 *   Нет (чистые константы)
 *
 * 🔹 ИСПОЛЬЗУЕТСЯ В:
 *   - apiClient.ts     → API_BASE (для построения URL)
 *   - auth.ts          → API_BASE
 *   - save.ts          → API_BASE
 *   - init.ts          → API_BASE (leaderboard, admin)
 *   - socket.ts        → API_BASE (преобразование в URL socket)
 *   - auth.ts          → SOCKET_COOLDOWN (если нужно)
 * ============================================================
 */

// ── API ────────────────────────────────────────────────────
// Базовый URL для всех API запросов к серверу.
// Используется как префикс: fetch(API_BASE + '/auth/tg')
// В production — относительный путь (тот же origin).
// В dev — проксируется Vite (vite.config.js: proxy → localhost:3000)
export const API_BASE = '/api';

// ── Socket ─────────────────────────────────────────────────
// Минимальная задержка (мс) между socket действиями
// (защита от спама PvP/трейд запросами)
export const SOCKET_COOLDOWN = 3000;
