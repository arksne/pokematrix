{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "rm -rf dist && npm run build"
  },
  "deploy": {
    "startCommand": "echo 'Server rebuild pending'",
    "restartPolicyType": "NEVER"
  }
}


